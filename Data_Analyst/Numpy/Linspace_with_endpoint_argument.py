import numpy as np

arrA = np.linspace(5, 20, 6, endpoint=False)

print(arrA)

# Syntax : arrname = np.linspace(start, end, no_element, argument)
# Default : endpoint = True
# The last element of the array isn't end.
